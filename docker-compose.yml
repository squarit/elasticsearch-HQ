version: "3"

services:
  hq:
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    ports:
     - "5000:5000"
    depends_on:
      - redis
  redis:
    build: ./deployment/redis
    expose:
      - "6379"
